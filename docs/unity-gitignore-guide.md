# Unity Git Ignore Guide

For Unity projects, keep source assets and project settings in Git while excluding generated folders such as Library, Temp, Logs, Obj, and local build output.

Using the standard Unity `.gitignore` template keeps repositories smaller and avoids committing machine-specific generated files.