# Branching Workflow

Create a short-lived branch for each feature or fix, commit the focused change, then open a pull request into `main`.

After the pull request is merged, start the next branch from the updated `main` branch to keep history easy to follow.