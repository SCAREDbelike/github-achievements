# Git Commit Basics

Prefer small commits that describe one logical change. Use short action-oriented messages such as `Add player movement` or `Fix pause menu input`.

Before committing, review changed files so generated files, secrets, and local editor data are not included accidentally.