# First Unity Project with GitHub

Create the Unity project first, initialize Git in the project folder, use the Unity `.gitignore`, make an initial commit, then connect the local repository to GitHub.

Commit `Assets`, `Packages`, and `ProjectSettings`; avoid generated folders such as `Library`, `Temp`, `Logs`, and `Obj`.